#include "InputManager.h"

//#include "Script.h"

void InputManager::init() {
    Script keys("scripts/keys.lua");
    //for(std::string* i = Commands;*i != "\0";++i) {
        //Button temp;

        //input_map.insert(map_item());
    //}
}
